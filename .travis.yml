language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t shavera/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true shavera/docker-react npm run test
